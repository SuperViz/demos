/// <reference types="forge-viewer" />
export declare class AvatarName {
    constructor(viewer: any, userId: any);
    private clock;
    private destPos;
    private curScreenPos;
    colorIndex: number;
    private readonly userId;
    private renderFrame;
    private viewer;
    private avatarModel;
    load(avatarModel: THREE.Mesh, name: any, colorIndex: any): void;
    private render;
    destroy(): void;
}
