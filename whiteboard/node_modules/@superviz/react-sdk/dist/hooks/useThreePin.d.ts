import { ThreeJsPin } from '@superviz/threejs-plugin';
import type { Camera, Object3D, Renderer, Scene } from 'three';
type UseThreeJsPin = {
    pin: ThreeJsPin | null;
    destroy: () => void;
};
type Params = {
    scene: Scene;
    renderer: Renderer;
    camera: Camera;
    player?: Object3D;
    controls?: any;
};
export declare function useThreeJsPin({ scene, renderer, camera, player, controls, }: Params): UseThreeJsPin;
export {};
//# sourceMappingURL=useThreePin.d.ts.map