import { PresenceMouseProps, Transform } from './types';
export declare class MousePointers {
    constructor(containerId: string, options?: PresenceMouseProps);
    transform(transform: Transform): void;
}
