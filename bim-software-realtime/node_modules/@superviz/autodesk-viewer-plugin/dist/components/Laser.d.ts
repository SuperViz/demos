/// <reference types="forge-viewer" />
/// <reference types="forge-viewer" />
import { Vector3 } from 'three';
import { Avatar } from './Avatar';
export declare class Laser {
    constructor(viewer: Autodesk.Viewing.GuiViewer3D, colorIndex: number, laserOrigin: Vector3);
    private clock;
    private lerpAnimationFrame;
    private viewer;
    private isDestroyed;
    private position;
    private quaternion;
    private mousePosition;
    private lerpedMousePosition;
    private colorIndex;
    private laserOrigin;
    mesh: THREE.Mesh;
    private avatar;
    private material;
    destroy(): void;
    setAvatar(avatar: Avatar): void;
    setMousePosition(position: THREE.Vector3): void;
    setColorIndex(index: number): void;
    private animate;
    private createTextureCanvas;
    private generateMaterial;
}
