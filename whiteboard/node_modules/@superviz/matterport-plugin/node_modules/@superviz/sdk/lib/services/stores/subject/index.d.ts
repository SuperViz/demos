import { BehaviorSubject } from 'rxjs';
import { PublicSubject } from '../common/types';
export declare class Subject<T> {
    state: T;
    private subject;
    private subscriptions;
    private showLog;
    constructor(state: T, subject: BehaviorSubject<T>, showLog?: boolean);
    private getValue;
    private setValue;
    subscribe: (subscriptionId: string | this, callback: (value: T) => void) => void;
    unsubscribe(subscriptionId: string): void;
    destroy(): void;
    expose(): PublicSubject<T>;
}
export default function subject<T>(initialState: T, showLog?: boolean): Subject<T>;
