import { Mesh, Vector3, Object3D, Scene, Camera } from 'three';
export declare class Laser {
    constructor(camera: Camera, scene: Scene, origin: Vector3);
    private clock;
    private destPos;
    private curPos;
    private startPos;
    private counter;
    private colorIndex;
    private lastColorIndex;
    private lastDestPos;
    private lastStartPos;
    origin: Vector3;
    private createdMaterial;
    private material;
    private lerpAnimationFrame;
    mesh: Mesh;
    root: Object3D;
    private tempStartVec;
    load(): void;
    update: any;
    private animate;
    destroy(): void;
    private createMaterial;
    private updateMesh;
    private createTextureCanvas;
}
