/// <reference types="forge-viewer" />
export declare class Mouse {
    constructor(viewer: any, userId: any);
    private clock;
    private destPos;
    private curScreenPos;
    colorIndex: number;
    private userId;
    private renderFrame;
    private viewer;
    private mousesWrapper;
    load(name: string, colorIndex: number): Promise<THREE.Object3D>;
    update: (destPos: THREE.Vector3, colorIndex?: number) => void;
    destroy(): void;
    private render;
    private renderDivWrapper;
}
