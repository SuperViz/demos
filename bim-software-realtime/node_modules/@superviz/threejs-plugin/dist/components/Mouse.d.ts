import { Vector3, Object3D, Scene, Sprite } from 'three';
import SpriteText from 'three-spritetext';
export declare class Mouse {
    constructor(scene: Scene);
    private clock;
    private destPos;
    private curPos;
    private counter;
    colorIndex: number;
    private lastColorIndex;
    private lastDestPos;
    origin: Vector3;
    private scene;
    private lerpAnimationFrame;
    textMesh: Object3D;
    arrowMesh: Object3D;
    root: Object3D;
    private backgroundMaterial;
    load(name: string, colorIndex: number): Promise<Object3D>;
    update: any;
    private animate;
    destroy(): void;
    createTextGeometry: (text: string, color: string) => SpriteText;
    createArrowGeometry: (colorIndex: number) => Promise<Sprite>;
}
