import { Object3D } from 'three';
import * as THREE from 'three';
import SpriteText from 'three-spritetext';
export declare class AvatarName {
    constructor(name: string, slot: number, height: number, scale: number);
    model: Object3D;
    private name;
    private color;
    private scale;
    private height;
    slot: number;
    text: SpriteText;
    load: () => Promise<Object3D<THREE.Event>>;
    createTextGeometry: (text: string, color: string) => SpriteText;
}
